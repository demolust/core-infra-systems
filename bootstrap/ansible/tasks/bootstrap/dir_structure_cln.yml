---
- name: Remove PXE directory structures
  ansible.builtin.shell:
    cmd: "rm -rf {{ item }}"
  loop:
    - "{{ base_dir }}/tftp/{{ selected_arch }}"
    - "{{ base_dir }}/www/{{ selected_arch }}"
    - "{{ base_dir }}/isos/"
    - "{{ base_dir }}"
  failed_when: false
