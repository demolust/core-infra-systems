---
- name: Create PXE directory structure
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
  loop:
    - "{{ base_dir }}"
    - "{{ base_dir }}/isos"
    - "{{ base_dir }}/tftp/{{ selected_arch }}"
    - "{{ base_dir }}/www/{{ selected_arch }}"
